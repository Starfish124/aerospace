# ADR-005: Terminal only for slice A

**Decision**: output is text in the terminal plus PNG files on disk. No web UI.
**Why**: keeps slice A to data and truth. A static HTML report is a later, one-hour upgrade.
