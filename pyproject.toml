[project]
name = "aerospace"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "lightkurve>=2.6.0",
    "pandas>=2.3.3",
    "pytest>=9.1.1",
]

[project.scripts]
aerospace = "transit.cli:main"

[tool.uv]
package = true

[tool.setuptools]
packages = ["transit", "rocket"]
